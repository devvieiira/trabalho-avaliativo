services:
  db:
    image: postgres:16
    container_name: database_postgres
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=12345678
      - POSTGRES_DB=estagio
    ports:
      - "5432:5432"
